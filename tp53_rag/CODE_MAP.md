# CODE_MAP — module reference

_Auto-generated by `tools/gen_code_map.py` (re-run to refresh)._ 
One entry per module: its purpose + public API. For architecture and data flow see [ARCHITECTURE.md](ARCHITECTURE.md); for the maths see [METHODS.md](METHODS.md).
**82 modules** across 7 packages.


## agents/

### `agents/adversarial_evidence.py`

Most RAG systems only retrieve evidence that SUPPORTS an answer. This layer does the opposite on purpose: it actively hunts for evidence that would CONTRADICT a proposed recommendation, then stages a bounded skeptic vs proposer exchange so…

- `viability_score(positive_matches, negatives, weights)` — V = S_match · (1 − min(1, Σ_k w_k·h_k)); h_k = min(1, count_k/CAP). S_match scales with the number of positive…
- `counterfactual_trials(mutation, cancer, matcher)` — Dual retrieval: recruiting matches (positive) + stopped trials (negative) for a variant, folded into a Viability Score.…
- `gather_contradicting_evidence(mutation, cancer, matcher)` — Bundle negative/contradicting signals: ClinVar conflicts + stopped trials. Returns a structured dict (never raises).
- `bounded_debate(proposal, contradicting_evidence, generate_fn, max_turns, role_generate)` — A HARD-CAPPED skeptic↔proposer exchange. Exactly min(max_turns, 2) LLM calls — never loops to convergence.…
- `adversarial_review(mutation, proposal, cancer, generate_fn, matcher)` — Full pass: gather contradicting evidence, then run the bounded debate. If no generate_fn is supplied, builds the…

### `agents/african_atlas.py`

- **class `AtlasProfile`** — Structured atlas response for one query.
- **class `AfricanTP53Atlas`** — Regional TP53 cancer-genomics atlas for African populations.  · methods: profile(mutation, region, cancer_type), country_burden()
- `atlas_profile(mutation, region, cancer_type)`

### `agents/african_drift.py`

- **class `DriftCheckResult`** — Result of African genomic drift check for one mutation.
- **class `AfricanDriftDetector`** — Detects potential racial bias in variant interpretation by cross-referencing against African population data. Runs…  · methods: check_mutations(mutations)

### `agents/auditor.py`

- **class `AuditResult`** — Structured output from the Auditor agent.
- **class `AuditorAgent`** — Agent #9 — The Medical Auditor. Every clinical output passes through here before reaching a clinician or patient. No…  · methods: audit(proposed_output, retrieved_sources, agent_name), audit_and_regenerate(proposed_output, retrieved_sources, regeneration_fn, agent_name)

### `agents/clinical_trials.py`

Finds active clinical trials relevant to a TP53 mutation + cancer type, prioritising trials with Kenyan / African sites (then international trials open to African patients). Offline-first design (same pattern as the ChEMBL client): * Live:…

- `parse_studies(payload)` — Parse a ClinicalTrials.gov v2 response into normalised trial records. Pure & defensive — tolerates None / missing keys,…
- **class `TrialSearch`** — 
- **class `ClinicalTrialsMatcher`** — Match TP53 mutation + cancer to recruiting clinical trials.  · methods: search(mutation, cancer_type, status, max_results, use_live, phases_234_only)
- `match_trials(mutation, cancer_type, use_live)`

### `agents/clinvar_conflict_checker.py`

- `normalize_significance(value)` — Map any significance phrasing onto the canonical vocabulary.
- `extract_mutations(text)` — Pull canonical TP53 protein mutations (e.g. R175H) from free text.
- **class `ConflictFinding`** — 
- **class `ClinVarConflictChecker`** — Flags AI classifications that disagree with ClinVar.  · methods: clinvar_classification(mutation), check(text, mutation, ai_classification)
- `check_conflicts(text, mutation, ai_classification)`

### `agents/command_center.py`

Aggregates the regional African TP53 atlas into a single decision-support snapshot: continental KPIs, per-region analytics (dominant cancers, key mutations, environmental drivers) and an honest resource/access layer for planners working in…

- **class `CommandCenterSnapshot`** — 
- **class `OncologyCommandCenter`** — Aggregate the African atlas into a command-center snapshot.  · methods: snapshot()
- `command_center_snapshot()` — Module-level convenience wrapper.

### `agents/confidence_consensus.py`

Instead of six specialists debating in prose, each one returns a *probability distribution* over a fixed set of management options, and the board's consensus is the aggregate of those distributions — a mathematical vote you can graph,…

- `parse_distribution(text, options)` — Extract a {letter: prob} distribution from a model reply. Returns (normalised_distribution, ok). On failure ->…
- `agent_distribution(member, mutation, case, options, generate_fn)` — One specialist's probability vote. Never raises.
- `convene_confidence_consensus(mutation, case, options, backend, generate_fn)` — Run the probability vote across all specialists and aggregate. Provide ONE of: `generate_fn` (used for every agent) —…

### `agents/digital_twin.py`

Explores how a case *might* unfold under different management strategies, using patterns drawn from published TP53 cohort literature — NOT a prediction for an individual patient. This is the honest form of a "digital twin": it surfaces…

- **class `Scenario`** — 
- **class `TwinExploration`** — 
- **class `DigitalTwinExplorer`** — Generate illustrative, evidence-grounded management scenarios.  · methods: explore(mutation, case)
- `explore_twin(mutation, case)` — Module-level convenience wrapper.

### `agents/dispatcher.py`

Precision Onco Africa - Multi-Agent Dispatcher Routes TP53 pipeline output to specialised agents (mutation, ORF, phylogenetics, domain, clinical, report, pathology, TNM staging) and aggregates their responses into a unified platform…

- **class `AgentResult`** — Structured result from a single agent.
- **class `AgentDispatcher`** — Dispatches TP53 pipeline results to multiple specialised agents and aggregates responses into a unified platform…  · methods: dispatch_single(agent_type, pipeline_data, custom_question), dispatch_all(pipeline_data, include_report, slide_image_path), interactive_query(question, pipeline_data), format_platform_output(results, include_sources)

### `agents/dossier_compiler.py`

Synthesizes outputs from all agents into: - Academic dossier (mechanistic pathways, biology) - Enterprise dossier (drug candidates, IP status, commercialization) Both formats exportable as markdown/PDF.

- **class `DossierOutput`** — Compiled dossier structure.
- **class `DossierCompiler`** — Synthesis engine for academic & enterprise reports.  · methods: compile(agent_results, mutation, cancer_type, mode), to_markdown(dossier)
- `compile_dossier(agent_results, mutation, cancer_type, mode)` — Convenience function.
- `to_markdown(dossier)` — Convert to markdown.

### `agents/drug_discovery.py`

- `score_mdm2_inhibitors(mutation_labels)` — Score MDM2 inhibitor applicability for each mutation. Returns structured, HL7-compatible result blocks.
- `flag_apr246_candidates(mutation_labels)` — Flag mutations eligible for APR-246 / PRIMA-1MET. Priority: R175H > R248W > others.
- `assess_synthetic_lethality(mutation_labels, co_mutations)` — Assess PARP inhibitor and synthetic lethality opportunities based on co-mutated genes.
- `build_keml_availability_report(mutation_labels)` — Build KEML availability report tailored to mutation profile.
- **class `DrugDiscoveryAgent`** — Agent #8 — Drug Discovery & Therapeutic Insights. Production-grade, HIPAA/HL7-compliant, scalable. All components are…  · methods: analyse(pipeline_data, co_mutations, patient_id), get_cache_stats(), clear_cache()

### `agents/enzyme_design.py`

== Designs p53 protein reactivation strategies, corrector peptides, and engineered enzyme variants targeting TP53 mutant-specific structural defects. Covers: - p53 DNA-binding domain (DBD) rescue strategies per mutation class - MDM2…

- **class `EnzymeDesignStrategy`** — 
- **class `EnzymeDesignResult`** — 
- **class `RateLimiter`** —   · methods: allow()
- `sanitise_query(query)`
- `sanitise_mutation(mutation)`
- `hash_patient_id(patient_id)`
- `get_design_strategy(mutation, co_mutations)`
- `build_fhir_report(patient_hash, strategy)` — HL7 FHIR R4 MedicationStatement / Procedure structured output.
- **class `EnzymeDesignAgent`** — TP53 Enzyme & Protein Engineering Agent. Usage ----- agent = EnzymeDesignAgent() result = agent.analyse( mutation =…  · methods: analyse(mutation, query, patient_id, co_mutations), compare_strategies(mutations), apr246_eligible(mutation)

### `agents/explainability.py`

Turns a TP53 assessment from an opaque output into a transparent evidence trace. For a given variant it answers "why?" by assembling, in one place: • the molecular classification and an earned confidence • supporting evidence lines…

- **class `EvidenceItem`** — 
- **class `Explanation`** — 
- **class `ExplainabilityEngine`** — Assemble a transparent evidence trace for a TP53 variant.  · methods: explain(mutation, case)
- `explain_variant(mutation, case)` — Module-level convenience wrapper.

### `agents/gemma_vision.py`

Uses Gemma 4's native multimodal capability (image + text -> text, via the Google GenAI backend) to read pathology slide images and photographed paper lab reports directly — no separate CNN foundation model (UNI/ResNet, torch/ timm) and no…

- **class `GemmaVisionAgent`** — Gemma-4-vision-backed reader for pathology images and lab report photos. Stateless aside from the lazily-constructed…  · methods: health(), read_pathology_slide(image_bytes, mime_type, mutation_data), read_lab_report_photo(image_bytes, mime_type), read_structure_snapshot(image_bytes, mime_type, mutation)

### `agents/gene_expression.py`

= Predicts gene expression changes and downstream cellular behaviour resulting from TP53 mutations. Integrates pathway analysis, transcription factor disruption, tumour microenvironment (TME) modelling, and Kenya-context clinical…

- **class `ExpressionProfile`** — 
- **class `GeneExpressionResult`** — 
- **class `RateLimiter`** —   · methods: allow()
- `sanitise_query(query)`
- `sanitise_mutation(mutation)`
- `hash_patient_id(patient_id)`
- `get_expression_profile(mutation, co_mutations)` — Return full expression + cellular behaviour profile for a TP53 mutation. co_mutations: list of co-occurring gene…
- `build_fhir_observation(patient_hash, profile)` — HL7 FHIR R4 Observation — gene expression analysis result.
- **class `GeneExpressionAgent`** — Gene Expression & Cellular Behaviour Prediction Agent. Usage ----- agent = GeneExpressionAgent() # Query by mutation…  · methods: analyse(mutation, query, patient_id, co_mutations), compare_mutations(mutations), synthetic_lethality_screen(mutation, co_mutations)

### `agents/immunogenicity.py`

Evaluates tumor microenvironment (TME) impact of TP53 mutations. Predicts immune infiltration status (hot/cold), checkpoint blockade response. Uses CIBERSORT/ESTIMATE-style immune scoring (mock implementation).

- **class `ImmunePrediction`** — Structured immune prediction output.
- **class `ImmunogenicityPredictor`** — Tumor microenvironment & immunotherapy response predictor.  · methods: predict(mutation, cancer_type, vaf)
- `predict_immune_response(mutation, cancer_type, vaf)` — Convenience function.

### `agents/ind_generator.py`

Generates a DRAFT FDA Investigational New Drug (IND) application skeleton from a TP53 mutation profile + candidate drug(s): preclinical summary, proposed mechanism of action, CMC stub, clinical-plan and safety sections. Offline-first &…

- **class `INDSection`** — 
- **class `INDDraft`** — 
- **class `INDGenerator`** — Assemble a draft IND skeleton from structured inputs.  · methods: generate(mutation, cancer_type, drug_candidates, sponsor), render_markdown(result)
- `generate_ind(mutation, cancer_type, drug_candidates)`

### `agents/kiswahili_hpo.py`

Maps colloquial Kiswahili symptom / phenotype descriptions to standardised clinical codes — Human Phenotype Ontology (HPO) and ICD-10 — so a clinician who enters an observation in Kiswahili gets it anchored to machine-readable clinical…

- `map_text(text, embed_fn, threshold)` — Map Kiswahili observation text to HPO/ICD-10 codes. Exact substring matches first (deterministic). If an embedder is…
- `to_clinical_terms(text, **kw)` — Convenience: a compact 'english (HPO/ICD-10)' string for prompt enrichment, or the original text if nothing mapped.

### `agents/liquid_biopsy.py`

= Analyses circulating tumour DNA (ctDNA) from liquid biopsy CSV data. Tracks VAF trends, flags resistance, scores treatment eligibility, and generates HL7-FHIR-structured clinical reports. HIPAA compliant — no raw PII in logs or LLM…

- **class `BiopsyRecord`** — 
- **class `ResistanceAlert`** — 
- **class `PatientSummary`** — 
- **class `AnalysisResult`** — 
- **class `RateLimiter`** —   · methods: allow()
- `sanitise_query(query)`
- `hash_patient_id(patient_id)`
- `load_csv(csv_path)` — Load and validate liquid biopsy CSV. Raises on bad schema.
- `classify_vaf(vaf)`
- `detect_resistance(records)` — Group records by patient+mutation, sort by date, flag VAF rises above RESISTANCE_DELTA_THRESHOLD as resistance events.
- `build_patient_summaries(records, alerts)`
- `build_fhir_report(summaries)` — HL7 FHIR R4 DiagnosticReport structure. All patient references use hashed IDs — HIPAA compliant.
- **class `LiquidBiopsyAgent`** — Liquid Biopsy ctDNA Analysis Agent. Parameters ---------- csv_path : Path Path to liquid_biopsy_tracker.csv ttl : int…  · methods: load(), analyse(query), get_patient(patient_hash), critical_alerts(), apr246_candidates(), stats()

### `agents/molecular_docking.py`

Estimates drug–protein binding for a TP53 mutation + candidate drug. Two honest modes (the active mode is ALWAYS reported in `method`): * "autodock_vina" — a real AutoDock Vina run (subprocess) when Vina is installed AND prepared PDBQT…

- `vina_available()` — True if an AutoDock Vina executable is on PATH.
- `parse_vina_output(text)` — Parse the best (mode 1) binding affinity (kcal/mol) from Vina stdout. Pure & defensive — returns None if no affinity…
- **class `DockingResult`** — 
- **class `MolecularDockingAgent`** — Dock a candidate drug against TP53 — real Vina or honest estimate.  · methods: dock(mutation, drug, pdb_id, mechanism, receptor_pdbqt, ligand_pdbqt, config)
- `dock_drug(mutation, drug, mechanism)`

### `agents/multilingual.py`

- **class `MultilingualReportAgent`** — Generates clinical and patient-facing reports in English and culturally nuanced Swahili.  · methods: generate(pipeline_data, agent_results, equity_context)

### `agents/multimodal_fusion.py`

Combines whatever evidence the clinician has gathered across the platform — a TP53 variant, a Gemma-vision pathology-slide narration, a photographed lab-report reading, a Sanger .ab1 finding, and free-text clinical notes — into a single,…

- `fuse_case(inputs, llm, generate_fn)` — Fuse the provided modalities into one unified summary. inputs: dict keyed by the modality keys in _MODALITIES. llm:…

### `agents/orthogonal_personas.py`

If every agent shares one base model at one temperature, a "debate" is theatre: they tend to agree because they share the same biases. To make multi-agent disagreement REAL, each role is given an orthogonal reasoning posture — a distinct…

- `persona_for(member)` — Case-insensitive lookup of a role's orthogonal posture. Never raises.
- `orthogonal_generate(backend, member, max_tokens)` — Return a generate_fn(system, user)->str bound to `member`'s orthogonal temperature / frequency penalty, so different…

### `agents/pathology_vision.py`

Agent #15 — Pathology Vision Agent Processes H&E slide images using UNI foundation model Correlates findings with TP53 mutation data CLOUD FIX: torch and timm are imported lazily inside _load_model() only when actually needed — not at…

- **class `PathologyVisionAgent`** — Agent #15 — Pathology slide analysis. Uses UNI foundation model for tissue embedding. Falls back to ResNet if UNI…  · methods: process_slide(image_path, mutation_data)

### `agents/pdf_report.py`

Precision Onco Africa - Clinical PDF Report Generator Professional printable report for tumor board meetings. Uses reportlab — pure Python, 100% offline.

- `generate_pdf_report(accession, pipeline_data, agent_results, multilingual_results, drug_results, audit_results, output_dir)`

### `agents/rag_chain.py`

- **class `PIIScrubber`** — Detects and redacts PII before any LLM call or log write. Replaces with SHA-256 hash references — traceable but not…  · methods: scrub(cls, text), hash_id(cls, raw_id)
- **class `RateLimiter`** —   · methods: allow(), wait_if_needed()
- **class `BM25`** — Lightweight BM25 implementation — no external deps. Optimised for biomedical exact terms: R175H, MDM2, APR-246, etc.  · methods: fit(docs), score(query, top_k)
- `compute_trust_weight(metadata)` — A transparent multiplicative trust prior applied at retrieval, so a retracted or superseded source is down-weighted…
- **class `CrossEncoderReranker`** — Reranks retrieved documents using a cross-encoder. Falls back to score-based reranking if model unavailable. Model…  · methods: rerank(query, candidates, top_k)
- `related_hotspots(mutation)` — Return curated related TP53 hotspots for cache warming. Pure.
- **class `SemanticCache`** — Caches LLM responses by query embedding similarity. Threshold ≥ 0.92 cosine similarity = cache hit. Uses SQLite for…  · methods: get(agent, query), set(agent, query, answer), warm(mutation, agent, generate_fn, max_related), stats()
- **class `ContextWindowManager`** — Manages token budgets for Gemma 2B (8192 token context). Truncates oldest history first, always preserves mutation +…  · methods: fit_context(cls, retrieved, history, max_chars), fit_history(cls, history, max_tokens), build_pipeline_str(cls, data, max_tokens)
- **class `ZeroResultHandler`** —   · methods: handle(agent_type, query, attempted_broadening)
- **class `ResponseValidator`** — Validates LLM responses for: - Non-empty output - Mutation name accuracy (no hallucinated variants) - Minimum length -…  · methods: validate(cls, response, agent_type, query), tighten_prompt(cls, original_question, attempt)
- **class `LlamaCppBackend`** — llama.cpp server client — connects to local llama-server. Expects: ./llama-server -m model.gguf -c 8192 --timeout 300…  · methods: generate(system_prompt, user_prompt, max_tokens, temperature), stream(system_prompt, user_prompt, max_tokens, temperature), health()
- **class `GoogleGenAIBackend`** — Google GenAI backend — Gemma 4 26B via API.  · methods: generate(system_prompt, user_prompt, max_tokens, **_), stream(system_prompt, user_prompt, max_tokens, **_), generate_vision(system_prompt, image_bytes, mime_type, user_prompt, max_tokens), health()
- **class `FireworksBackend`** — Fireworks AI backend — OpenAI-compatible chat completions served on AMD Instinct GPUs. Used for the AMD-accelerated…  · methods: generate(system_prompt, user_prompt, max_tokens, temperature, frequency_penalty), stream(system_prompt, user_prompt, max_tokens, temperature), health()
- **class `OllamaBackend`** — Ollama backend — local on-device inference.  · methods: generate(system_prompt, user_prompt, max_tokens, **_), stream(system_prompt, user_prompt, max_tokens, **_), health()
- **class `IntentRouter`** —   · methods: route(query)
- **class `AuditLogger`** —   · methods: log(cls, event)
- **class `HybridSearchEngine`** — Standalone hybrid search: vector (0.7) + BM25 (0.3) fusion. Isolated so vector_store or BM25 failures never crash the…  · methods: search(query, k), format_context(docs_with_scores)
- **class `TP53RAGChain`** — Orchestrator only — delegates all work to specialist classes. If any component fails, the others keep running.…  · methods: query(question, pipeline_data, agent_type, k, conversation_history), query_stream(question, pipeline_data, agent_type, conversation_history), batch_query(questions, pipeline_data), cache_stats()

### `agents/sanger_ab1.py`

Reads a real Sanger sequencing trace (.ab1 / ABIF format) with Biopython and turns it into: QC metrics, the base-called sequence, heterozygous (double-peak) site detection from the raw trace, and — if a reference segment is supplied —…

- `parse_ab1(data)` — Parse ABIF bytes into sequence, quality, trace channels and peaks. Never raises — returns {"success": False, "error":…
- `qc_metrics(quality)` — Read-quality summary. Pure; never raises.
- `detect_heterozygous_sites(sequence, trace, peaks, base_order, min_ratio)` — Flag positions where a secondary trace peak rivals the called base — the classic Sanger signature of a heterozygous…
- `call_variants(read_seq, read_quality, reference, min_quality)` — Call variants of the read against a reference segment via global pairwise alignment. Reports substitutions, insertions…
- `analyze_ab1(data, reference)` — Full pipeline: parse → QC → heterozygous sites → (optional) variant calls vs a reference. Never raises.
- `synthesize_ab1(sequence, quality, het_sites)` — Build a minimal but valid ABIF (.ab1) byte string for `sequence`. quality: optional per-base PHRED (default 40,…

### `agents/structural_analyzer.py`

Analyses the structural consequences of a TP53 mutation for drug design: * ΔΔG — thermodynamic destabilisation of the DNA-binding domain * binding-pocket / cavity descriptors (volume, hydrophobicity, druggability) * residue-contact context…

- **class `StructuralProfile`** — 
- **class `StructuralAnalyzer`** — Curated structural-mechanics + cavity analysis for TP53 mutations.  · methods: analyse(mutation)
- `analyse_structure(mutation)`

### `agents/structure_rescue.py`

The "virtual wet-lab" feature. Gemma (the architect) proposes a second-site suppressor for an oncogenic p53 mutation; a real generative fold is run on an AMD Instinct MI300X (ESMFold, facebook/esmfold_v1); and the geometry is measured so…

- `structures_available()` — True only when all three precomputed ESMFold structures are present.
- `load_pdb(variant)`
- `parse_ca(pdb_text)` — Return (Cα coordinates, per-residue pLDDT) parsed from PDB ATOM records. pLDDT is read from the B-factor column;…
- `mean_plddt(pdb_text)` — Mean pLDDT on a 0–100 scale (normalising ESMFold's 0–1 B-factors).
- `kabsch_rmsd(P, Q)` — Kabsch-aligned RMSD over Cα coordinates (the structural 'warp').
- `structural_rescue_analysis()` — Assemble the honest analysis from the precomputed real structures. Never raises; returns ``available: False`` when…
- `hypothesis_prompt(mutation)` — (system, user) prompt for Gemma to propose a suppressor — the architect step.
- `gemma_interpret(analysis, generate_fn)` — Let Gemma read the measured geometry and explain it. Injected generate_fn; returns None on any failure (graceful).
- `rescue_overlay_html(wt_pdb, mutant_pdb, mutation_resi, height)` — Self-contained 3Dmol.js overlay: wild-type as a green ghost, mutant solid in amethyst, with the mutated residue…

### `agents/structure_snapshot.py`

Renders a 2D snapshot of the p53 alpha-carbon backbone — with the patient's mutated residue highlighted — entirely SERVER-SIDE (matplotlib/Agg, no WebGL, no GPU), then hands that PNG to Gemma 4 vision. Instead of describing the structure…

- `mutation_codon(mutation)` — Extract the residue number from a mutation like 'R175H'. None if absent.
- `parse_ca_coords(pdb_text)` — Parse alpha-carbon (CA) coordinates keyed by residue number from PDB text. Fixed-column parse; tolerant; never raises.
- `render_snapshot(pdb_text, highlight_resi, mutation, title)` — Render the Cα backbone as a PNG (bytes), highlighting `highlight_resi`. Returns None if there is nothing to draw. Never…
- `analyze_structure(mutation, pdb_text, gemma_agent, use_live)` — Full pipeline: fetch/accept a p53 structure → render the snapshot with the mutated residue highlighted → let Gemma…

### `agents/structure_viz/agent.py`

- **class `StructureVisualisationAgent`** — Agent #7 — 3D Structure Visualisation. Optimized for consumer hardware (8GB RAM). Default: PDB fallback (fast) + ESM-2…  · methods: run(pipeline_data, use_esmfold, generate_embeddings)

### `agents/structure_viz/predictor.py`

- **class `MutationSite`** — A single mutation to visualise on the structure.
- **class `StructureResult`** — Full output from the structure visualisation agent.
- `apply_mutations(sequence, mutations)` — Apply a list of point mutations to a protein sequence.
- `predict_structure_esmfold(sequence)` — Predict 3D structure using ESMFold. Returns PDB format string or None if model unavailable.
- `fetch_pdb_structure(accession)` — Fallback: fetch known p53 PDB structure. 2OCJ = human p53 DBD with DNA. Works offline if already cached, otherwise…
- `generate_esm2_embeddings(sequence, mutations)` — Generate per-residue ESM-2 embeddings for wildtype and mutant sequences. Returns (wt_embeddings, {mutation_label:…
- `reduce_to_3d(embeddings, n_components)` — Reduce high-dimensional ESM-2 embeddings to 3D using UMAP. Falls back to PCA if UMAP unavailable.
- `extract_plddt(pdb_string)` — Extract per-residue pLDDT confidence scores from ESMFold PDB output.
- `classify_mutations(mutations_raw)` — Convert raw pipeline mutation dicts to MutationSite objects with hotspot classification.

### `agents/structure_viz/web_app.py`

- `index()`
- `get_structure_data()`
- `get_wildtype_pdb()`
- `get_mutant_pdb(label)`
- `launch(structure_output, accession, port)` — Launch the web app with the given structure output data. Called automatically after structure analysis completes.

### `agents/surgical_brief.py`

- **class `SurgicalBriefAgent`** — Agent #10 — Pre-Incision Surgical Brief. Generates a high-contrast, large-font tactical briefing designed for operating…  · methods: generate(pipeline_data)

### `agents/synthetic_lethality.py`

Identifies synthetic-lethal (SL) targets for TP53-mutant tumours — genes whose inhibition selectively kills p53-deficient cells (the tumour) while sparing p53-wild-type normal tissue. Offline-first & curated: a sourced SL knowledge base…

- **class `SLResult`** — 
- **class `SyntheticLethalityModeler`** — Rank synthetic-lethal targets for a TP53-mutant context.  · methods: model(mutation, min_evidence)
- `model_synthetic_lethality(mutation)`

### `agents/tnm_staging.py`

- **class `TNMComponent`** — Single TNM component with evidence and confidence.
- **class `TNMResult`** — Complete TNM staging output.
- **class `TNMStagingAgent`** — Agent #16 — TNM Staging & Clinical Roadmap Generator. Takes PathologyVisionAgent output + TP53 mutation data and…  · methods: stage(pathology_result, pipeline_data)
- `get_tnm_agent(rag_chain)` — Get or create the global TNM agent instance.
- `stage_cancer(pathology_result, pipeline_data, rag_chain)` — Convenience function matching dispatcher pattern.

### `agents/tumor_board.py`

Convenes a virtual multidisciplinary tumour board for a TP53-mutant case. A panel of specialist members (Pathologist, Geneticist, Oncologist, Surgeon, Pharmacologist, Equity Officer) each forms an opinion from the variant's established…

- **class `MemberOpinion`** — 
- **class `BoardConsensus`** — 
- **class `VariantProfile`** — 
- `parse_variant(mutation)` — Parse a protein change (e.g. 'R175H', 'p.R248Q', 'R213*') into a structured profile. Defensive — unparseable input…
- **class `TumorBoard`** — Convene a simulated multidisciplinary tumour board for a TP53 case.  · methods: convene(mutation, case)
- `convene_tumor_board(mutation, case)` — Module-level convenience wrapper.

### `agents/uncertainty.py`

The legitimate version of "MC-dropout / token-entropy" uncertainty for a hosted model we don't control: sample the SAME prompt N times at temperature > 0 and measure how much the answers actually agree. Low agreement = the model is not…

- `band(uncertainty)`
- `epistemic_uncertainty(samples, embed_fn)` — Compute the Epistemic Uncertainty Index over N sampled answers. Fewer than 2 samples → uncertainty is undefined…
- `sample_and_measure(system_prompt, user_prompt, generate_fn, embed_fn, n)` — Sample the prompt n times, measure epistemic uncertainty, and return a representative answer with the uncertainty band.…

### `agents/variant_curator.py`

Parses genomic variants, cross-references ClinVar/COSMIC/gnomAD, classifies mutation pathogenicity (LOF/GOF/DNM). Output: Structured JSON with variant class, clinical significance, databases.

- **class `VariantClassification`** — Structured variant classification output.
- **class `VariantCurator`** — Expert variant classification engine.  · methods: classify(mutation_input)
- `classify_variant(mutation)` — Convenience function.

## utils/

### `utils/alphafold_client.py`

Fetches the real AlphaFold-predicted structure of human p53 (UniProt P04637) and its per-residue confidence (pLDDT, stored in the PDB B-factor column). Offline-first (same pattern as the other API clients): * Fetched server-side (no…

- `parse_plddt(pdb_text)` — Parse per-residue pLDDT (CA B-factor) from PDB text. Pure, never raises. Returns {per_residue: {resSeq: plddt}, mean,…
- `plddt_band(score)`
- **class `AlphaFoldStructure`** — Real AlphaFold structure + confidence. Always populated (never raises).  · methods: to_dict()
- **class `AlphaFoldClient`** — Fetch the AlphaFold structure for a UniProt accession, with caching.  · methods: get_structure(uniprot, use_live)
- `get_tp53_structure(use_live)`

### `utils/amd_benchmark.py`

Serves the results of a real benchmark run on AMD hardware (produced once by `tools/benchmark_amd.py` on the AMD Developer Cloud) and the honest deployment map shown in the UI. Design mirrors the precompute-and-serve pattern used…

- `load_benchmark(path)` — Load the AMD benchmark JSON. Never raises; returns an honest 'available: False' state when the file is absent or…

### `utils/autonomic.py`

A self-healing ops layer: it watches real system memory (psutil, everywhere) and real AMD GPU telemetry (rocm-smi / amd-smi, when an AMD host is present), and when memory crosses a threshold it takes a GENUINE reclaim action (garbage…

- `system_stats()` — Real host memory + CPU via psutil. Never raises.
- `gpu_stats()` — Real AMD GPU telemetry when rocm-smi/amd-smi exist; an honest 'not present' otherwise. NEVER fabricates numbers. Never…
- **class `AutonomicManager`** — Threshold-driven self-healing. Reclaimers are injected (name, callable) so the app wires real ones (clear semantic…  · methods: status(), self_heal(reclaimers, force)

### `utils/chembl_client.py`

Provides real drug data for compounds targeting the TP53 pathway (p53 reactivators, MDM2/MDMX inhibitors) from the ChEMBL database. Offline-first design: * A curated set of real TP53-pathway drugs (name, mechanism, clinical phase) ships…

- `parse_mechanisms(payload, target)` — Parse a ChEMBL /mechanism response into normalised drug records. Pure & defensive — tolerates None / missing keys,…
- **class `ChEMBLClient`** — Fetch TP53-pathway drugs from ChEMBL, with caching + curated fallback.  · methods: fetch_target_drugs(target, limit), compounds(targets, use_live, limit)
- `tp53_pathway_drugs(use_live)`

### `utils/clinic_memory.py`

When a clinician corrects an answer, that correction is saved locally and injected as HIGH-PRIORITY context into future prompts for related questions — so the system "learns" from this specific clinic's expert without any model weight…

- **class `ClinicMemory`** — JSON-backed store of clinician corrections + relevance retrieval.  · methods: add(question, correction, tags), clear(), all(limit), relevant(query, limit), as_prompt_block(query, limit), stats()

### `utils/codegraph.py`

Builds a real dependency graph of the platform's own source — nodes are modules, edges are internal imports — and lays the nodes out along a DNA double helix. The result is both a genuine architecture map and the project's signature…

- `build_codegraph(root)` — Return {nodes, links} for the internal import graph. Never empty.
- `helix_layout(graph, radius, rise, turn)` — Attach (x, y, z) coordinates placing nodes on a DNA double helix. Even-indexed nodes go on strand A, odd on strand B…
- `build_helix_codegraph(root)` — Convenience: build the import graph and apply the helix layout.

### `utils/export_disclaimer.py`

- `stamp_markdown(content, title)` — Return *content* with a RUO header and footer block. Always returns a non-empty string, even if *content* is None/empty…
- `stamp_dict(payload)` — Return a deep copy of *payload* with RUO metadata keys injected. Never mutates the caller's object. If *payload* is not…
- `stamp_json(payload, indent)` — Serialise *payload* to JSON with RUO metadata embedded. Falls back to a string repr for non-serialisable values so a…
- `stamp_fhir(resource)` — Attach the RUO notice to a FHIR R4 resource without breaking schema. The disclaimer is added two ways, both…

### `utils/guardrails.py`

Every answer passes through two independent gates before it reaches a user: answer → 1. SYNTACTIC guardrail (well-formed, non-empty, no error/echo markers) → 2. SCIENTIFIC guardrail (claims cross-checked against ClinVar) → confidence →…

- **class `GateResult`** — 
- `run_guardrails(answer, mutation)` — Run both gates and return a combined verdict. Never empty, never raises.

### `utils/hardware_probe.py`

Reports, honestly, what compute backend the platform is actually running on: AMD ROCm GPU, an NVIDIA/CUDA GPU, or CPU-only. It probes the real environment (ROCm install, torch HIP/CUDA build, env flags) and never claims acceleration that…

- `detect_compute()` — Probe the host for the real compute backend. Never raises. Returns a dict with: accelerator…
- `log_compute_banner(logger)` — Log the honest compute banner at startup and return the probe result. Uses the given logger (loguru or stdlib). The…

### `utils/hybrid_search.py`

Combines lexical (BM25) and semantic (vector) search. Weighted fusion: 0.7 vector + 0.3 BM25 for best recall.

- **class `SearchResult`** — Single search result with metadata.
- **class `HybridSearchEngine`** — Fusion of BM25 lexical + vector semantic search.  · methods: build_bm25_index(documents), vector_search(query_embedding, doc_embeddings, top_k), bm25_search(query, top_k), fused_search(query, query_embedding, doc_embeddings, top_k), query(q, query_embedding, doc_embeddings, top_k)

### `utils/logger.py`

- `setup_logger()`

### `utils/memory.py`

Precision Onco Africa — long-term conversation memory. Persists past Q&A turns to a local SQLite database so a conversation can resume across app restarts ("doesn't start from zero") instead of living only in volatile session state.…

- **class `ConversationMemory`** — SQLite-backed, PII-scrubbed conversation memory. Args: db_path: SQLite file. Defaults to data/conversation_memory.db.…  · methods: remember(session_id, question, answer, agent_type), recent(session_id, limit), history_strings(session_id, limit), rolling_summary(session_id, max_chars), clear(session_id), stats()
- `get_memory()`

### `utils/microfluidic.py`

An intelligent fluidics quality-control policy for a liquid-biopsy workflow: it watches per-frame quality telemetry from the microfluidic channel and, the moment it detects an unrecoverable fault (a bubble, an occlusion, collapsing droplet…

- **class `FrameVerdict`** — 
- `analyze_run(frames, total_planned, compute_per_frame_s)` — Run the QC policy over a sequence of telemetry frames. Aborts at the first hard fault and reports the sequencing…
- `demo_scenarios()` — Two illustrative runs: a clean pass and a bubble-fault early-abort.

### `utils/mock_hardware.py`

A software mock of a portable genomic-sequencer's device API — the way real firmware teams develop *before* the hardware arrives. It does NOT pretend the hardware exists; every response is explicitly flagged ``mock: True`` and the UI…

- **class `DeviceState`** — 
- **class `MockSequencer`** — Deterministic mock of a portable sequencer's control API. Every method returns a structured response carrying ``mock:…  · methods: snapshot(), open_door(), insert_sample(barcode), scan_barcode(), lock_and_focus(), arm(), advance_run(fraction), reset()
- `run_mock_demo_sequence()` — Drive the device through a full clean lifecycle (for tests/demo). Returns the final telemetry after a complete run.

### `utils/offline_status.py`

Reports, honestly, which capabilities work with NO internet connection and which require it. This is the substance behind the "offline cancer copilot" claim: in a low-connectivity clinic the core analysis still runs; only the…

- `offline_capabilities()` — Return the readiness map plus a count summary. Never empty.

### `utils/pii_scrubber.py`

Regex-based PII detection + SHA-256 hashing. Preserves data traceability while removing sensitive info.

- **class `ScrubStats`** — Audit trail for PII scrubbing.
- **class `PIIScrubber`** — HIPAA-compliant PII redaction engine.  · methods: hash_value(value), scrub_text(text, mode), scrub_dict(data, skip_keys), scrub_json_string(json_str), reset_stats()
- `scrub(text, mode)` — Convenience function for one-off scrubbing.
- `scrub_dict(data, skip_keys)` — Convenience function for dict scrubbing.

### `utils/pubmed_citations.py`

Fetches real PubMed references for a TP53 mutation / claim via the NCBI Entrez E-utilities (esearch + esummary) and formats them as inline [PMID: …] citations with links to the abstract. Offline-first & honest: * Live: real PMIDs + titles…

- `pubmed_search_url(term)`
- `pubmed_abstract_url(pmid)`
- `format_citation(rec)` — Inline citation string, e.g. 'Smith et al., Nature 2020 [PMID: 123]'.
- `parse_esearch(payload)` — Extract PMIDs from an esearch JSON response. Defensive.
- `parse_esummary(payload)` — Parse esummary JSON into citation records. Defensive.
- **class `PubMedClient`** — Query PubMed for citations, with caching + a search-pointer fallback.  · methods: cite(query, max_results, use_live)
- `pubmed_cite(query, max_results, use_live)`

### `utils/rag_cache.py`

utils/cache.py — Shared SQLite Query Cache Reusable across all agents. Thread-safe, TTL-based, HIPAA-compliant. No PII stored — keys are SHA-256 hashes of sanitised queries.

- **class `CacheDB`** — Thread-safe SQLite query cache. Usage: cache = CacheDB() # default path + TTL cache = CacheDB(ttl=3600) # 1-hour TTL…  · methods: get(agent_id, query), set(agent_id, query, value), delete(agent_id, query), flush_agent(agent_id), flush_all(), stats()
- `get_cache(db_path, ttl)` — Return (or create) the module-level default CacheDB instance.

### `utils/reranker.py`

Takes top-20 results from search and reranks to top-5 using a lightweight cross-encoder model. Improves relevance of final LLM context.

- **class `RankedDocument`** — Document with rerank score.
- **class `CrossEncoderReranker`** — Lightweight reranking using a cross-encoder-style scorer. In production, this would use sentence-transformers: from…  · methods: rerank(query, documents, top_k, diversity_filter), rerank_and_merge(query, search_results, top_k)

### `utils/security.py`

Central, reusable defences applied at every untrusted boundary: file uploads, free-text that reaches the LLM, filenames, and content destined for HTML components. Pure functions, no I/O, fully unit-tested with adversarial inputs. Threat…

- `is_probably_binary(data, sniff)` — Heuristic: null bytes or a high non-text ratio ⇒ binary (e.g. an executable disguised as a .vcf). True ⇒ reject as a…
- `has_dangerous_magic(data)` — True if the file starts with known executable/archive magic bytes.
- `safe_filename(name)` — Strip directories, control chars and traversal sequences from a filename. Always returns a non-empty, path-safe…
- `validate_upload(data, filename, max_bytes, allowed_ext, require_text)` — Validate an uploaded file's size, extension, and content nature. Returns {ok, reason, friendly} — never raises.…
- `looks_like_vcf(text)` — True if the text has VCF structure: a ## header or a tab/space-delimited line whose 2nd column is an integer position.
- `detect_prompt_injection(text)` — Flag (do not silently rewrite) likely prompt-injection attempts.
- `sanitize_for_prompt(text, max_chars)` — Neutralise role-tag injection and cap length before text reaches the LLM. Conservative: strips control chars and fake…

### `utils/shared_state.py`

Shared state manager for multi-agent communication. Replaces text-passing between agents with direct state sharing.

- **class `SharedAgentState`** — Thread-safe shared state for all 14 agents. Agents read/write directly — no text serialization.  · methods: update(key, value), get(key, default), update_agent_output(agent_name, output), get_all_outputs(), reset()

### `utils/token_router.py`

Most AI systems optimise for accuracy. This one also optimises for *cost*: it decides, before any expensive call, the cheapest route that can still answer a query correctly — cache hit → 0 LLM tokens (reuse a prior answer) deterministic…

- `estimate_tokens(text)` — Transparent ~4-chars-per-token estimate. Never negative.
- `decide_route(query, cache_hit, force_llm)` — Pure routing decision for a query. Returns the route, a human reason, and the estimated LLM tokens avoided by not…
- **class `RouterStats`** — 
- **class `TokenRouter`** — Stateful router that records decisions and accumulates savings.  · methods: route(query, cache_hit, force_llm), report(), reset()
- `get_router()`

### `utils/variant_annotation.py`

Turns a TP53 variant (protein change like R175H, an rsID, or HGVS) into a real, multi-source functional annotation: * Molecular consequence + SIFT / PolyPhen — Ensembl VEP REST API * ClinVar significance, gnomAD allele freq, CADD, dbNSFP…

- **class `AnnotationResult`** — Structured, source-tagged variant annotation. Never empty.  · methods: to_dict()
- `normalise_protein_change(text)` — Extract a canonical protein change key (e.g. 'R175H') from free text.
- `parse_vep(payload)` — Parse an Ensembl VEP REST response. Pure, defensive, never raises.
- `parse_myvariant(payload)` — Parse a MyVariant.info response (ClinVar/gnomAD/CADD/dbNSFP). Pure.
- **class `VariantAnnotator`** — Real multi-source TP53 variant annotation with offline-first fallback.  · methods: annotate(variant, use_live)
- `annotate_variant(variant, use_live)` — Convenience: annotate and return a plain dict (UI/JSON friendly).

### `utils/variant_effect.py`

Serves real ESM-2 protein-language-model variant-effect scores **offline, with no torch at runtime**. How it works ------------ The heavy compute (running the ESM-2 transformer over every TP53 substitution) is done ONCE, ahead of time, by…

- **class `VariantEffect`** — ESM-2 variant-effect result. Always populated (never raises).  · methods: to_dict()
- `parse_variant(text)` — Return (wt, position:int, mut) from e.g. 'R175H' / 'p.R175H', or None.
- **class `VariantEffectPredictor`** — Loads a precomputed ESM-2 matrix and serves offline lookups.  · methods: available(), predict(variant), stats()
- `get_predictor()`
- `predict_effect(variant)` — Convenience: predict and return a plain dict.

### `utils/vcf_parser.py`

Accept real patient VCF files (v4.1 / v4.2) as input instead of manual mutation entry. Filters to the TP53 locus (chr17p13.1) and returns a structured mutation list compatible with the pipeline_data format. Honest annotation policy: * The…

- `is_tp53_locus(chrom, pos)` — True if a genomic coordinate falls within the TP53 locus (either build).
- `extract_protein_change(info)` — Pull a 1-letter protein change (e.g. R175H) from an INFO annotation.
- `extract_cdna_change(info)`
- `parse_vcf_text(text, tp53_only)` — Parse VCF text into structured records. Never raises. Returns {variants, total_lines, tp53_count, skipped, columns_ok}.…
- `parse_vcf_bytes(data, tp53_only)` — Parse VCF from raw bytes (e.g. an uploaded file). Decodes UTF-8 leniently. Enforces an upper size bound before decoding…
- `sample_vcf()` — A small demo VCF (GRCh38) for the UI — one hotspot, one annotated, one unannotated TP53 variant, plus a non-TP53 line…

### `utils/viz.py`

Precision Onco Africa — Visualization helpers. Pure, Streamlit-free functions so they can be unit-tested without a running app. app.py imports these; tests import these. Everything returns a non-empty result (honours the platform's…

- `agent_status_badge(name, state, elapsed)` — Return HTML for a single live agent status badge. state: 'running' | 'complete' | 'failed'. Always returns a non-empty…
- `animated_vaf_timeline(days, vafs, mrd_threshold)` — Animated ctDNA VAF reveal with an MRD threshold line. Markers turn green when VAF falls (responding) and red when it…
- `animated_hotspot_bar(codons, freqs)` — Animated 'build-up' bar chart of TP53 hotspot frequencies (Reds scale).
- `build_agent_graph_data(agent_names)` — Build {nodes, links} for the 3D multi-agent graph. Pure data, never empty. Central `Dispatcher` → `RAG Core` → every…
- `agent_graph_3d_html(graph_data, height)` — Self-contained HTML for an interactive 3D force-directed agent graph. Uses the `3d-force-graph` WebGL library (drag to…
- `variant_annotation_table(result)` — Render a real-variant annotation dict (utils.variant_annotation) as a clean two-column table. Pure + never-empty.
- `variant_effect_gauge(result)` — Gauge for an ESM-2 variant-effect result (utils.variant_effect). Shows the masked-marginal log-likelihood ratio (more…
- `agent_architecture_diagram(agent_names, spin_revolutions)` — Radial agent network with an animated 'data flow' dispatch. The dispatcher sits at the centre; each agent is a node on…
- `domain_legend_chart()` — Horizontal domain map that doubles as the colour legend for the 3D view.
- `needle_plot(variants, title)` — Lollipop / needle plot of mutations along the p53 protein (1–393). *variants* is an iterable of dicts; each needs at…
- `parse_residues(raw, extra)` — Parse a residue string into a sorted list of unique ints (1–393). Non-numeric tokens are dropped — this also sanitises…
- `protein_viewer_html(pdb_id, residues)` — Self-contained 3Dmol.js viewer: domain-coloured cartoon, auto-rotating, hotspot residues shown as labelled red spheres.…
- `alphafold_viewer_html(pdb_text, residues, mean_plddt, height)` — Self-contained 3Dmol.js viewer for a real AlphaFold model, coloured by the standard pLDDT confidence scheme (blue=very…
- `mutation_structure_html(pdb_text, mutation, height)` — Mutation-aware 3D structure viewer. Colours the p53 fold by domain, highlights the *patient's* mutated residue in gold…
- `plddt_profile_chart(per_residue, mean_plddt)` — Per-residue AlphaFold pLDDT confidence profile. Pure + never-empty.
- `pathogenicity_gauge(significance, confidence)` — Benign -> Pathogenic gauge for a variant classification. Never empty.
- `tme_donut(t_cell_fraction, immune_status)` — Tumour-microenvironment composition donut (T-cell vs other). Never empty.
- `vaf_gauge(vaf, mrd_threshold)` — Current ctDNA VAF as a burden gauge against the MRD threshold. Never empty.
- `pathway_diverging_bar(activated, suppressed)` — Diverging bar of activated (red, +) vs suppressed (blue, -) p53 pathways.
- `synthetic_lethal_network(sl_result)` — Radial TP53-centric synthetic-lethal network: TP53 at the centre, SL target genes around it; edge/node colour by…
- `ind_section_chart(ind_result)` — IND draft section coverage — each section green (drafted) or amber (placeholder / to-be-completed). Never empty.
- `structural_profile_radar(struct_result)` — Radar of a mutation's structural-mechanics profile: destabilisation (ΔΔG), druggability, cavity volume and pocket…
- `docking_affinity_gauge(docking_result)` — Binding-affinity gauge (kcal/mol) for a docking result. More negative = stronger binding (green); the docking method is…
- `vcf_variant_chart(variants)` — TP53 variants from a VCF by QUAL, coloured by significance (hotspot = red, annotated = blue, unannotated = grey). Never…
- `trials_priority_chart(trials)` — Recruiting trials by clinical phase, coloured by geographic priority (Kenya = gold, other African = teal, international…
- `confidence_consensus_chart(result)` — Board's aggregated probability vote over the management options — the 'mathematical consensus' view. Winning option…
- `chembl_phase_chart(compounds)` — Horizontal bar of TP53-pathway compounds by clinical phase (0-4). Coloured green→red by maturity (Approved = green).…
- `clinvar_conflict_chart(findings)` — Dumbbell chart: AI vs ClinVar classification per mutation. Each row shows the AI's call (◆) and ClinVar's call (●) on a…
- `african_atlas_map(country_burden)` — Africa choropleth coloured by representative TP53-cancer burden (0-100). `country_burden`: {country_name: score}. Never…
- `african_burden_bar(matched_profiles)` — Horizontal bar of relative burden per matched African TP53 profile.
- `tnm_stage_bar(stage_group)` — Horizontal stage-progression gauge (I → IV) with the patient's stage highlighted. Colour runs green (early) → red…
- `mutation_class(mutation)` — Classify a TP53 variant into a mechanism-relevant bucket. Returns one of: y220c, zinc, contact, conformational, other.…
- `dock_candidates(mutation)` — Rank candidate drugs for a mutation by illustrative binding affinity. More-negative affinity = stronger predicted…
- `docking_affinity_chart(candidates)` — Horizontal bar of illustrative binding affinity (kcal/mol). Lower (more negative) = stronger binding; the top bar is…
- `docking_pose_html(pdb_id, residues, drug_name, affinity)` — Illustrative 3Dmol docking pose: protein cartoon + highlighted binding pocket + a translucent ligand cloud over the…
- `tumor_board_html(board, height)` — Self-contained HTML for the Live AI Tumour Board — specialist cards with confidence meters, a sequentially-revealed…
- `explainability_panel_html(exp, height)` — Self-contained HTML for the Explainability 'Why?' trace — headline classification with confidence, evidence lines…
- `amd_benchmark_chart(bench)` — Bar chart of real AMD-hardware benchmark runs (TFLOP/s and latency). Honest placeholder when the benchmark has not been…
- `deployment_panel_html(tiers)` — Honest 'Current ✓ / Future •' deployment map. Current = runnable today; future = roadmap targets, never claimed as…
- `command_center_html(snapshot, height)` — Decision-support dashboard: continental KPI tiles + per-region analytics cards with dominant cancers, key mutations,…
- `offline_readiness_html(status)` — Render the offline-capability map: each capability tagged offline (green) or needs-network (amber). Honest,…
- `codegraph_helix_html(graph, height)` — Self-contained WebGL render of the codebase as a DNA double helix: modules are glowing spheres on two spiralling…
- `agent_eval_table(report)` — Render per-agent evaluation metrics as a table. Never empty.
- `token_router_chart(report)` — Donut of how queries were routed (cache / deterministic / LLM), with the avoided-call share. Never empty.
- `guardrails_html(verdict)` — Compact verdict strip: the two gates (form + fact) with pass/fail, plus the overall gate and confidence. Pure,…
- `mock_device_html(demo)` — Edge-sequencer control panel: lifecycle stages lighting up + telemetry. Clearly labelled a simulated device interface.…
- `microfluidic_html(run)` — Per-frame channel quality strip with the abort/continue decision and the sequencing compute saved. Labelled a simulated…
- `scenario_explorer_html(exploration)` — Render illustrative management scenarios as cards. Each carries its evidence basis, confidence and caveat — and the…

### `utils/voice_conversation.py`

Powers the "Talk to Gemma" panel: a hands-light, multi-turn spoken conversation where the clinician speaks a question, Gemma answers (with the running conversation as context), Jarvis reads it back, and a polite sign-off is understood. Two…

- `detect_dismiss_intent(text)` — True when the clinician is politely ending the conversation. Matches explicit closing phrases (e.g. "thank you Gemma,…
- `faster_whisper_available()`
- `transcribe_fast(audio_path, language)` — Transcribe an audio file with faster-whisper. Raises on failure so the caller can fall back to openai-whisper.

### `utils/voice_output.py`

Spoken responses, the lightweight way: the platform speaks its answers using the browser's built-in Web Speech API (``speechSynthesis``). That means the audio is generated in the *user's* browser — no server-side TTS engine, no extra…

- `is_speakable(text)` — True if there is meaningful, non-whitespace text to speak.
- `speak_html(text, autoplay, rate, label)` — Return a self-contained HTML/JS control that speaks *text* in-browser. Always returns non-empty HTML. If there is…
- `speak_html_bargein(text, rate)` — Jarvis speaks the answer AND politely yields if interrupted — a human barge-in. Detection is 100% ON-DEVICE: it watches…

### `utils/voice_transcriber.py`

- **class `VoiceTranscriber`** — Speech-to-text using OpenAI Whisper (local or API).  · methods: transcribe(audio_bytes, language), transcribe_from_file(filepath), transcribe_streamlit_audio()
- `transcribe_clinical_query(audio_bytes, model)` — Quick function to transcribe clinical voice query. Args: audio_bytes: Audio file bytes model: Whisper model size…

## knowledge_base/

### `knowledge_base/ingestion.py`

- **class `TP53DocumentIngester`** — Ingests medical data safely using standard request pools and split maps.  · methods: load_curated_knowledge(), load_ncbi_gene_summary(), load_uniprot_annotation(), load_user_documents(directory), chunk_documents(documents), ingest_all(include_ncbi, include_uniprot, include_user_docs)

### `knowledge_base/pathway_graph.py`

Standard RAG retrieves prose; the biological *relationships* between p53 and its targets get lost in it. This module encodes well-established TP53 pathway relationships as explicit (subject → predicate → object) triples and renders them as…

- `triple_text(t)`
- `related_concepts(query, max_n)` — Graph traversal: for each pathway triple whose one endpoint is mentioned in `query`, return the OTHER endpoint's…
- `expand_query_keywords(query, max_n)` — Append graph-neighbour concept names to a query (for keyword search). Returns the query unchanged when nothing related…
- `pathway_enrichment_text()` — One combined, retrievable block of the p53 relationship graph.
- `pathway_documents()` — Return the pathway graph as document dicts ({content, metadata}) ready to become LangChain Documents. One combined…

### `knowledge_base/vector_store.py`

- **class `TP53VectorStore`** — Persistent local vector DB wrapper with cached connection parameters.  · methods: build(documents, force_rebuild), load(), is_built(), similarity_search(query, k, score_threshold), filtered_search(query, category, k), get_retriever(k), get_stats()

## config/

### `config/settings.py`

_(no public API)_

## api/

### `api/server.py`

- **class `PipelineData`** — TP53 pipeline output passed to agents.
- **class `AnalyseRequest`** — Request for full multi-agent platform dispatch.
- **class `QueryRequest`** — Request for a single free-form query.
- **class `AgentQueryRequest`** — Request for a specific agent.

## tools/

### `tools/benchmark_amd.py`

Run ONCE on the AMD Developer Cloud (or any ROCm host) to produce real inference/throughput numbers, then commit data/amd_benchmark.json so the app can display them offline. What it measures (whatever is available on the host): 1. Device…

- `device_report()` — Probe the host for ROCm/HIP + GPU details. Degrades gracefully.
- `matmul_benchmark(size, iters)` — Sustained large matmul — reports TFLOP/s actually achieved on device.
- `llm_latency(prompt, max_tokens)` — Round-trip latency against the active inference backend (optional). max_tokens defaults to the app's real CTX_RESPONSE…
- `vllm_throughput(model, prompts, max_tokens)` — Measure generation throughput (tokens/sec) with vLLM on ROCm. vLLM serves models on AMD Instinct via ROCm; this batches…
- `main()`

### `tools/gen_code_map.py`

Walks the codebase and emits CODE_MAP.md — one entry per module with its purpose (module docstring) and public API (top-level classes/functions with signatures + one-line summaries). A fast way to re-orient in the project months later…

- `module_entry(path)`
- `main()`

### `tools/precompute_esm2.py`

Precompute ESM-2 variant-effect scores for TP53 (one-time, torch-enabled host). Runs the ESM-2 protein language model over every possible single-residue substitution in TP53 using *masked-marginal* scoring, and writes a small JSON matrix…

- `fetch_tp53_sequence()` — Fetch the canonical TP53 protein sequence from UniProt (P04637).
- `main(argv)`

### `tools/validate_n8n.py`

Verifies n8n_workflow.json is a coherent, fully-wired automation graph WITHOUT needing a running n8n daemon: every node reachable, a webhook trigger present, the FastAPI hand-off (httpRequest) nodes present, and an audit trail (writeFile)…

- `validate_workflow(path)` — Load + structurally validate the workflow. Never raises.
- `main()`

### `tools/whitepaper_to_pdf.py`

Build a print-ready PDF from WHITEPAPER.md. Pure-Python (markdown + xhtml2pdf) so it runs offline with no system dependencies. Regenerate any time the whitepaper changes: python tools/whitepaper_to_pdf.py Output: WHITEPAPER.pdf (next to…

- `build()`

## benchmarks/

### `benchmarks/agent_eval.py`

Continuously benchmarks the deterministic decision agents (tumour board, explainability) on metrics that can be measured honestly without a live LLM: • latency — mean wall-clock per case • success rate — fraction returning a valid,…

- `evaluate_tumor_board(cases)` — Evaluate the tumour board over the case set.
- `evaluate_explainability(cases)` — Evaluate the explainability engine over the case set.
- `run_agent_eval()` — Run the full agent-evaluation suite. Never empty.

### `benchmarks/run_benchmark.py`

TP53 Variant Curator — accuracy benchmark. Runs the curated ground-truth variants through the existing VariantCurator and scores its structured output against ClinVar/IARC reference classifications. Pure offline: VariantCurator.classify()…

- `load_ground_truth(path)` — Load and lightly validate the ground-truth file. Returns a dict with 'metadata' and 'variants'. On any failure returns…
- `run_benchmark(classifier, ground_truth)` — Execute the benchmark. Args: classifier: mutation -> classification-dict. Defaults to VariantCurator. Injectable so…
- `render_markdown(report)` — Render a human-readable markdown report. Always returns a non-empty string.
- `save_report(report, results_dir)` — Persist JSON + markdown reports. Returns the written paths.
- `main(argv)`

### `benchmarks/scoring.py`

Benchmark scoring helpers — pure functions, no I/O, no LLM. Kept separate from the runner so they can be unit-tested in isolation. Every function tolerates None / missing / unexpected values and never raises on bad input (honours the…

- `normalize_significance(value)` — Lower-case, whitespace/dash-normalised significance string. Maps common synonyms onto the canonical vocabulary. Unknown…
- `significance_bucket(value)` — Collapse a significance value into pathogenic_leaning / benign_leaning / uncertain.
- `score_variant(predicted, expected)` — Score one variant prediction against its expected record. Returns a non-empty result dict; missing fields degrade to…
- `aggregate(results)` — Aggregate per-variant results into overall metrics. Reports exact accuracy, concordant (bucketed) accuracy, IARC…
