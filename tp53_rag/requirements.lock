# ============================================================
# TP53 RAG Platform — Pinned dependency lock (reproducible build)
# Direct dependencies pinned to the EXACT versions from the last known-good
   Streamlit Cloud build (Python 3.11). Use this file for reproducible /
# audited deploys; `requirements.txt` remains the flexible (bounded) install.
#
# To use on Streamlit Cloud: rename/point the app to this file.
# To regenerate a FULL transitive lock on the deploy target (recommended
    before launch): on the Python 3.11 environment run
#     pip install -r requirements.txt && pip freeze > requirements.lock
# (cloud-safe block only — keep torch/sentence-transformers/whisper out).
# ============================================================

# UI
streamlit==1.58.0

# RAG & Vector DB
chromadb==0.5.23
langchain==0.3.30
langchain-core==0.3.86
langchain-community==0.3.31
langchain-chroma==0.1.4

# Document processing
pypdf==6.12.2
beautifulsoup4==4.14.3
requests==2.34.2

# Bioinformatics
biopython==1.87

# Data & visualization
pandas==2.3.3
numpy==1.26.4
plotly==5.24.1

# Search & ranking
rank-bm25==0.2.2
scipy==1.17.1
scikit-learn==1.9.0

# API server
fastapi==0.136.3
uvicorn==0.48.0
httpx==0.28.1
python-multipart==0.0.30

# Protobuf — kept as a bound (the exact post-pin resolution varies; regenerate
# with pip freeze on the deploy env for an exact value). Must stay < 6 to avoid
# breaking chromadb's opentelemetry _pb2 import.
protobuf>=4.25.0,<6.0.0

# Inference (cloud) + embeddings
google-genai==2.7.0
huggingface-hub==0.36.2

# Utilities
python-dotenv==1.2.2
pydantic==2.13.4
rich==15.0.0
tqdm==4.67.3
loguru==0.7.3
pygments==2.20.0
markdown-it-py==4.2.0
mdurl==0.1.2

# Testing
pytest==9.0.3
pytest-asyncio==1.4.0
